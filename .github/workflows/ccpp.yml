name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: make Builder
      run: |
        cd Creational/Builder
        make
    - name: run Builder
      run: |
        cd Creational/Builder
        make run
  
